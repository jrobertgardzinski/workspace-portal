# Postgres for the comments service — its own instance, dev-parity with compose
# (comments-postgres). Data on a local-path PVC (k3s' default provisioner);
# strategy Recreate because the volume is ReadWriteOnce.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: comments-postgres-data
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: comments-postgres
  labels:
    app: comments-postgres
    app.kubernetes.io/part-of: portal
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: comments-postgres
  template:
    metadata:
      labels:
        app: comments-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:18-alpine
          ports:
            - name: pg
              containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: comments
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: portal-db
                  key: POSTGRES_PASSWORD
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres", "-d", "comments"]
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres", "-d", "comments"]
            initialDelaySeconds: 20
            periodSeconds: 10
            failureThreshold: 6
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              memory: 256Mi
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: comments-postgres-data
---
apiVersion: v1
kind: Service
metadata:
  name: comments-postgres
spec:
  selector:
    app: comments-postgres
  ports:
    - name: pg
      port: 5432
      targetPort: pg
