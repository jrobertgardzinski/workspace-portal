# user-collections from the IDE, the rest of the stack in Docker (see memes.env for the idea).
COLLECTIONS_PROFILE=dev
COLLECTIONS_PORT=8092
DB_URL=jdbc:postgresql://localhost:5436/collections
DB_USER=postgres
DB_PASSWORD=secret
SECURITY_URL=http://localhost:8080
KAFKA_BOOTSTRAP_SERVERS=localhost:29092
